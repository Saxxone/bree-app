import { FetchMethod, ApiConnectService } from "./ApiConnectService";
import api_routes from "@/constants/ApiRoutes";

// export async function login(username: string, password: string) {
//   return await ApiConnectService({ url: api_routes.login, method: FetchMethod.POST, content_type: "application/json", body: { username, password }, key: "login" });
// }
